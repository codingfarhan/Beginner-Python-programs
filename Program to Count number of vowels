#this program counts the number of vowels contained in the string entered by the user
count=0
f=input('alright enter a string')
k=[]
k=f
for s in k:
   if s=='a':
      count+=1
   elif s=='e':
        count+=1
   elif s=='i':
        count+=1
   elif s=='o':
        count+=1
   elif s=='u':
        count+=1
   else:
     continue

   print('There are ', count, ' number of vowels in the string')
   input('enter any key to exit')
